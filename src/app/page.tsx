/**
 * Leads Table Page
 *
 * Build a <LeadsTable /> component that:
 * 1. Renders 10,000 leads with infinite scroll
 * 2. Search input (debounced, server-side via API)
 * 3. Status dropdown filter (server-side via API)
 * 4. Clickable column headers for sorting (server-side via API)
 * 5. Loading and error states
 * 6. BONUS: AI enrichment column with per-row "Enrich" button
 *
 * See README.md for full details.
 */
 
export default function Home() {
  return (
    <main>
      <h1>Leads Table</h1>
      {/* TODO: implement <LeadsTable /> */}
    </main>
  );
}
 